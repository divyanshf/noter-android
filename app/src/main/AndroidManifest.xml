<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.noter">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".NoterApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_local_library_24_yellow"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_local_library_24_yellow"
        android:supportsRtl="true"
        android:theme="@style/Theme.Noter">
        <activity android:name=".ui.settings.SettingsActivity" />
        <activity android:name=".ui.edit.EditActivity" />
        <activity android:name=".ui.auth.AuthActivity" />
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>